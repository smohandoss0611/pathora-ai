# Streamlit Community Cloud installs from this file.
# The heavy optional extras (Postgres, Redis, Pinecone, MCP) are deliberately
# omitted: the deployed demo runs on SQLite + the in-memory vector store.
pydantic>=2.7
pydantic-settings>=2.2
langgraph>=0.2
langchain-core>=0.3
PyMuPDF>=1.24
httpx>=0.27
streamlit>=1.37
